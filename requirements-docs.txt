# Dependências do SITE de documentação apenas (mkdocs.yml + scripts/build_docs_site.py).
# O plugin não ganha dependência nenhuma: ele roda com o Python embutido do QGIS.
#   pip install --user -r requirements-docs.txt
mkdocs~=1.6
mkdocs-material~=9.5
pymdown-extensions~=10.7
