# Python
__pycache__/
*.py[cod]
*.pyo

# QGIS arquivos gerados
*.gpkg
*.qgs~
*.qgz~
*.bak
sig_bus/resources_rc.py

# Dados gerados em runtime (não são código-fonte)
sigt.gpkg
gtfs_corrigido.zip
*.gpkg-shm
*.gpkg-wal

# Pacotes de build (gerados por `make zip`/`make package`, não código-fonte)
dist/

# Site de documentação: saída do MkDocs e páginas derivadas geradas por
# `scripts/build_docs_site.py` (decisão 160 — a fonte é `sig_bus/*.md`)
site/
docs/guias/
docs/referencia/
docs/arquitetura/
docs/changelog.md
docs/visao-geral.md
docs/en/

# Sistema
.DS_Store
Thumbs.db

# --- Arquivos de trabalho dos agentes (NAO versionar: ficam locais/no brain repo privado) ---
CLAUDE.md
AGENTS.md
GEMINI.md
ACTIONS.md
INSTRUCTIONS.md
QUICK_START_OSM.md
PLAN.md
docs/topicos/
.planexec/
.claude/
.idea/
